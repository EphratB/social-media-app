import appName from "../Includes/variables";

function Header() {
  return (
    <div>
      <h1>{appName}</h1>
      <header className="header">Header</header>
    </div>
  );
}
export default Header;
